# SKILL: 创建通用技能

**触发**：编写技能、新建 skill、创建技能文件、添加技能。

## 核心原则

技能是**通用的任务指引**，描述"做什么、怎么做"，与任何特定 Agent 前端无关。

- **禁止** 使用 `---` YAML 头（那是 VS Code Copilot 专用格式）
- **禁止** 出现 `user-invocable`、`argument-hint` 等前端专属字段
- **禁止** 引用特定 Agent 工具名称（如"使用 xxx 工具"）
- **应当** 只描述流程、步骤、规范，任意 AI 助手均可理解和执行

## 文件规范

- **位置**：`.agents/SKILLS/<name>.md`
- **格式**：纯 Markdown，无 YAML 头
- **命名**：小写 + 连字符，如 `write-tests.md`、`pre-commit-check.md`

## 内容结构

```markdown
# SKILL: <中文简短名称>

**触发**：<关键词列表，用于 AI 匹配何时加载此技能>。

## <章节>

<具体步骤、规范、注意事项>
```

### 必备元素

| 元素 | 说明 |
| - | - |
| `# SKILL:` 标题 | 技能名称，简洁明了 |
| `**触发**：` | 关键词列表，AI 据此判断何时使用此技能 |
| 步骤/规范 | 具体的可执行内容 |

## 编写清单

1. 确认技能目标单一、聚焦（一个技能只做一件事）
2. 编写清晰的触发关键词
3. 步骤具体可执行，不依赖特定工具
4. 保持通用性：Linux / Windows / macOS 均可执行
5. 创建文件后在 `AGENT.md` 的目录树和技能表中注册

## 示例

参考已有技能：

- `pre-commit-check.md` — 步骤型，每步一条命令
- `publish.md` — 流程型，含概览和注意事项
- `write-tests.md` — 规范型，规定测试约定
